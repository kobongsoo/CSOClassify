#------------------------------------------------------------------
# MpowerClassify 패키지 초기화
#=> 한국어 문서를 텍스트 추출하고 임베딩 벡터를 출력하는 로컬 CLI 도구의
#   최상위 패키지다. 버전 문자열만 노출하고, 실제 기능은 하위 모듈에 있다.
#
# -필드: __version__ = 패키지/실행파일 버전 (데몬 핸드셰이크 호환성 판단에 사용)
# -필드: PROCESS_START_PERF = 프로세스(파이썬 진입) 시작 시각. startup 시간 측정 기준점.
#------------------------------------------------------------------

import time as _time

# 이 패키지가 '처음 import 되는 순간' = 사실상 프로세스 시작 직후(런처/‘python -m’ 두 진입점
# 모두 여기가 가장 이른 공통 지점). 이 값을 기준으로 startup(진입~첫 추출) 시간을 잰다.
# ※ 파이썬 진입 이전(onedir 부트로더의 _internal 로딩 등)은 파이썬에서 측정 불가.
PROCESS_START_PERF = _time.perf_counter()

__version__ = "0.1.0"
